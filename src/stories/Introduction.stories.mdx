import { Meta } from "@storybook/addon-docs"

<Meta title="Introduction" />

# React Component Library Template

Welcome to this template project for a React component library. This project provides an example story for a button component. The button has a single control "label", which is shown with two example values.
